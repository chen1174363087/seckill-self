<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenxin.dao.OrderMapper">
    <insert id="insertBatchSelf" parameterType="com.chenxin.entity.Order">
        INSERT INTO `order`(product_id, username, telphone, address, status)
        VALUES
        <foreach collection="list" index="index" item="order" separator=",">
            (#{order.productId},#{order.username},#{order.telphone},#{order.address},#{order.status})
        </foreach>
    </insert>
</mapper>
